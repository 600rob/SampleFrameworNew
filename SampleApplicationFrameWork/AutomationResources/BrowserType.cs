namespace SampleApplicationFrameWork
{
    public enum BrowserType
        //use this enum to set the browser type when we want to start a browser instance for a test
        //from the basePage
    {
        Chrome
    }
}